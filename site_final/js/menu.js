/*(function () {
    "use strict"
    var icone = document.getElementById("icone");
    console.log(icone)

    function ouvrirmenu(){
        this.classList.toggle(".menuClose")
    }

    for (let x = 0; x<icone.length; x++) {
        let click = icone[x];
        click.addEventListener("click", ouvrirmenu)
    }
}())
*/